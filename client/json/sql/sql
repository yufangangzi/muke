wget et https://repo.mysql.com//my//m//mysql80-community-release-el7-1.noarch.rpm
yum
yum install -y -y mysql80-community-release-el7-1.noarch.rpm
yum
yum install -y mysql-community-server
service mysql start (or: systemctl start mysqld)
# 查看密码
grep password /var/log/og/mysqld.log
mys
mysql -uroot -p

# 忘记密码，重置
1. Open & Edit /etc/tc/my.cnf
2.f
2. Add skip-grant-tables under [mysqld]
3. Restart Mysql
4. You should be able to login to mysql now using the below command mysql -u root -p
5. Run mysql> flush privileges;
6. Set new password by ALTER USER 'root'@''@'localhost' ID' IDENTIFIED BY 'NewPassword';
7. Go back to /etc/tc/my.cnf anf and remove/comment skip-grant-tables
8. Restart Mysql
9. Now you will be able to login with the new password mysql -u root -p

# 允许特定ip账号登陆
use mysql;
GRANT ALL PRIVILEGES ON *.* TO 'root'@''@'192.168.2.3' ID' IDENTIFIED BY 'root'  WITH GRANT OPTION;
flush privileges;
